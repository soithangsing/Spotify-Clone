let btn = document.getElementById('lib-button')
let sidebar = document.querySelector('.nav-left')

btn.onclick = () => {
    sidebar.classList.toggle('active')
}

// Make a timer to set disappear for scrollbar after some time and reappear when hover above it

// Make tooltips